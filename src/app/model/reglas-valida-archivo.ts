export interface validaArchivo{
    id              : number, 
    archivos        : string[],
    extension       : string[],
    size            : number[],
    formatoFecha    : string
}
export interface archivos{
    nombre          : string;
}
export interface extension{
    nombre          : string;
}
